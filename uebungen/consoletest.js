const msg = [4, 5, 6]

console.time(); //gibt Zeit aus die für den Vorgang benötight wird
console.warn('content'); //gibt content als Warnung in der Konsole aus
console.error('content'); //gibt content als Fehler in der Konsole aus

for (let i = 0; i < 100; i++) {
    console.log(i)
}

console.log("hello World")
console.table(msg) //gibt Tabelle aus
